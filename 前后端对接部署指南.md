# RiskSmart 前后端对接部署指南

## 一、已完成的改动

### 1. 后端改动

#### 1.1 新增 VO 类
- `MetaVo.java` - 路由元数据
- `RouterVo.java` - Vue路由配置
- `TreeSelect.java` - 树形选择组件

#### 1.2 新增 Mapper
- `SysMenuMapper.java` + `SysMenuMapper.xml` - 菜单数据访问
- `SysRoleMapper.java` + `SysRoleMapper.xml` - 角色数据访问

#### 1.3 新增 Service
- `ISysMenuService.java` + `SysMenuServiceImpl.java` - 菜单业务逻辑
- `ISysRoleService.java` + `SysRoleServiceImpl.java` - 角色业务逻辑

#### 1.4 新增/增强 Controller
- **SysMenuController.java** (新增)
  - `GET /system/menu/getRouters` - 获取前端路由配置 ✨核心接口
  - `GET /system/menu/list` - 菜单列表
  - `GET /system/menu/{menuId}` - 菜单详情
  - `GET /system/menu/treeselect` - 菜单树
  - `POST /system/menu` - 新增菜单
  - `PUT /system/menu` - 修改菜单
  - `DELETE /system/menu/{menuId}` - 删除菜单

- **SysRoleController.java** (新增)
  - `GET /system/role/list` - 角色列表
  - `GET /system/role/{roleId}` - 角色详情
  - `POST /system/role` - 新增角色
  - `PUT /system/role` - 修改角色
  - `PUT /system/role/dataScope` - 数据权限
  - `PUT /system/role/changeStatus` - 修改状态
  - `DELETE /system/role/{roleIds}` - 删除角色

- **SysUserController.java** (增强)
  - `GET /system/user/getLoginInfo` - 获取登录用户信息 ✨核心接口
  - `GET /system/user/getInfo` - 获取用户详情
  - `PUT /system/user/resetPwd` - 重置密码
  - `PUT /system/user/changeStatus` - 修改状态
  - `GET /system/user/authRole/{userId}` - 获取授权角色
  - `PUT /system/user/authRole` - 用户授权
  - `GET /system/user/deptTree` - 部门树

- **SysDeptController.java** (增强)
  - `GET /system/dept/treeselect` - 部门树选择
  - `GET /system/dept/roleDeptTreeselect/{roleId}` - 角色部门树
  - `GET /system/dept/list/exclude/{deptId}` - 排除节点的部门列表

### 2. 前端改动

#### 2.1 代理配置
**文件**: `risk-smart-web/web/vue.config.js`
```javascript
proxy: {
  '/dev-api': {
    target: 'http://localhost:8080', // 改为本地网关
    changeOrigin: true,
    pathRewrite: {
      ['^/dev-api']: '',
    },
  },
}
```

#### 2.2 路由清理
**文件**: `risk-smart-web/web/src/router/config.js`

已注释掉本次版本不存在的模块：
- `Backtrack` (流程回溯)
- `Monitor` (监测预警)
- `ApprovalAuth` (审批授权)

### 3. 网关配置

**文件**: `deploy/mysql/init/06-nacos-config-data.sql`

配置的路由规则：
```yaml
routes:
  # 认证服务
  - id: risk-smart-auth
    uri: lb://risk-smart-system
    predicates:
      - Path=/login,/logout,/captchaImage

  # 系统服务 (需手动添加 /vm/system/** 路由)
  - id: risk-smart-system
    uri: lb://risk-smart-system
    predicates:
      - Path=/system/**

  # 数据中台
  - id: risk-smart-data-vm
    uri: lb://risk-smart-data-middle-station
    predicates:
      - Path=/vm/smartData/**
    filters:
      - StripPrefix=2

  # 决策管理
  - id: risk-smart-decision-vm
    uri: lb://risk-smart-decision-manage
    predicates:
      - Path=/vm/smartDecision/**
    filters:
      - StripPrefix=2
```

---

## 二、部署步骤

### 步骤 1: 更新数据库

执行 Nacos 配置初始化脚本：
```bash
cd D:\WorkSpace\Code\RiskSmart-OpenSource\deploy\mysql\init
mysql -h localhost -u root -p < 06-nacos-config-data.sql
```

### 步骤 2: 手动配置 Nacos 网关路由

登录 Nacos 控制台 `http://localhost:8848/nacos` (nacos/nacos)

编辑配置 `risk-smart-gateway-dev.yml`，在 routes 列表中添加：

```yaml
# 系统服务 - /vm/system/** -> 去掉 /vm 前缀
- id: risk-smart-system-vm
  uri: lb://risk-smart-system
  predicates:
    - Path=/vm/system/**
  filters:
    - StripPrefix=1

# 登录日志 - /vm/logininfor/**
- id: risk-smart-logininfor-vm
  uri: lb://risk-smart-system
  predicates:
    - Path=/vm/logininfor/**
  filters:
    - StripPrefix=1

# 认证服务 - /vm/login 等
- id: risk-smart-auth-vm
  uri: lb://risk-smart-system
  predicates:
    - Path=/vm/login,/vm/logout,/vm/captchaImage
  filters:
    - StripPrefix=1
```

同时在 `security.ignore.whites` 中添加：
```yaml
security:
  ignore:
    whites:
      - /login
      - /logout
      - /captchaImage
      - /vm/login
      - /vm/logout
      - /vm/captchaImage
```

### 步骤 3: 启动后端服务

确保 Docker 中间件运行（MySQL、Redis、Nacos）：
```bash
docker ps
```

启动后端服务：
```bash
cd D:\WorkSpace\Code\RiskSmart-OpenSource\risk-smart-backend

# 启动网关
cd risk-smart-gateway
mvn spring-boot:run

# 启动系统服务
cd ../risk-smart-system
mvn spring-boot:run

# (可选) 启动数据中台
cd ../risk-smart-data-middle-station
mvn spring-boot:run

# (可选) 启动决策管理
cd ../risk-smart-decision-manage
mvn spring-boot:run
```

验证服务注册：
- 访问 `http://localhost:8848/nacos` 
- 查看 "服务管理 > 服务列表"
- 确认 `risk-smart-gateway` 和 `risk-smart-system` 已注册

### 步骤 4: 启动前端

```bash
cd D:\WorkSpace\Code\RiskSmart-OpenSource\risk-smart-web\web
npm install
npm run serve
```

访问 `http://localhost:8081`

---

## 三、测试验证

### 3.1 登录流程测试

1. 访问前端登录页
2. 输入用户名密码登录
3. **关键验证点**:
   - 网络请求: `POST /dev-api/login` → 网关路由到 `/login`
   - 响应包含 `token`

### 3.2 菜单加载测试

登录成功后自动调用：
- `GET /dev-api/vm/system/menu/getRouters` → `/vm/system/menu/getRouters` → `/system/menu/getRouters`
- `GET /dev-api/vm/system/user/getLoginInfo` → `/vm/system/user/getLoginInfo` → `/system/user/getLoginInfo`

**验证点**:
- 左侧菜单正常显示
- 用户信息正常显示

### 3.3 系统管理功能测试

点击 "系统管理" 菜单，测试：
- **用户管理**: 列表查询、新增、编辑、删除、重置密码
- **角色管理**: 列表查询、新增、编辑、删除、分配权限
- **菜单管理**: 树形展示、新增、编辑、删除
- **部门管理**: 树形展示、新增、编辑、删除

---

## 四、常见问题

### 4.1 登录后白屏 / 菜单不显示

**原因**: getRouters 接口未正确配置

**解决**:
1. 检查 Nacos 网关配置是否包含 `/vm/system/**` 路由
2. 检查 `SysMenuController` 的 `/getRouters` 接口是否存在
3. F12 查看网络请求，确认路径映射是否正确

### 4.2 接口返回 404

**原因**: 网关路由配置错误

**解决**:
1. 检查前端调用的 URL 前缀 (`/vm/`, `/dev-api/`)
2. 检查网关路由的 `Path` 匹配规则
3. 检查 `StripPrefix` 配置是否正确

### 4.3 接口返回 401 未授权

**原因**: Token 未正确传递或校验失败

**解决**:
1. 检查 `security.ignore.whites` 白名单配置
2. 检查前端是否正确存储和传递 token
3. 检查 SecurityUtils 是否正确解析 token

### 4.4 数据中台/决策管理接口 404

**原因**: 相关服务未启动或路由配置错误

**解决**:
1. 确认 `risk-smart-data-middle-station` 服务已启动并注册到 Nacos
2. 确认 `risk-smart-decision-manage` 服务已启动并注册到 Nacos
3. 检查网关的 `/vm/smartData/**` 和 `/vm/smartDecision/**` 路由配置

---

## 五、后续扩展建议

### 5.1 缺失的业务接口

以下接口前端有调用但后端未实现，需根据业务需求补充：

**用户信息扩展**:
- `/system/userInfo/sendSmsAuthCode` - 发送短信验证码
- `/system/userInfo/getUserAuth` - 获取用户认证信息
- `/system/userInfo/submit` - 提交用户信息

**企业认证**:
- `/system/userEnterprise/authSubmit` - 企业认证提交
- `/system/userEnterprise/list` - 企业列表
- `/system/userEnterprise/authStatus` - 认证状态

**站内消息**:
- `/system/messageInside/list` - 消息列表
- `/system/messageInside/delete` - 删除消息

### 5.2 性能优化

1. **Redis 缓存**: 
   - 菜单数据缓存 (减少数据库查询)
   - 用户权限缓存

2. **接口权限**:
   - 完善 `@PreAuthorize` 注解
   - 实现细粒度权限控制

3. **日志增强**:
   - 操作日志完整记录
   - 登录日志详细记录

---

## 六、路由映射总结

| 前端调用 | 代理后 | 网关路由 | 后端接收 | 服务 |
|---------|--------|---------|---------|------|
| `/dev-api/login` | `/login` | 直通 | `/login` | risk-smart-system |
| `/dev-api/vm/system/menu/getRouters` | `/vm/system/menu/getRouters` | StripPrefix=1 | `/system/menu/getRouters` | risk-smart-system |
| `/dev-api/vm/system/user/getLoginInfo` | `/vm/system/user/getLoginInfo` | StripPrefix=1 | `/system/user/getLoginInfo` | risk-smart-system |
| `/dev-api/vm/smartData/interfaceManage/list` | `/vm/smartData/interfaceManage/list` | StripPrefix=2 | `/interfaceManage/list` | risk-smart-data-middle-station |
| `/dev-api/vm/smartDecision/process-policy/list` | `/vm/smartDecision/process-policy/list` | StripPrefix=2 | `/process-policy/list` | risk-smart-decision-manage |

---

**部署完成后，系统应具备以下功能**:
✅ 用户登录/登出
✅ 动态菜单加载
✅ 用户管理 CRUD
✅ 角色管理 CRUD
✅ 菜单管理 CRUD
✅ 部门管理 CRUD
✅ 字典管理 (如果数据准备好)
✅ 数据中台接口调用
✅ 决策管理接口调用

祝部署顺利！ 🎉
