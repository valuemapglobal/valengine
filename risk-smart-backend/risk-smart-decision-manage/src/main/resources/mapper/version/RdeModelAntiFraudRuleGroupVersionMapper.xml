<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.value.decision.version.mapper.RdeModelAntiFraudRuleGroupVersionMapper">

    <!-- 批量插入反欺诈模型规则组版本 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO rde_model_anti_fraud_rule_group_version (
            id, model_id, name, descr, status, remark, create_user_id, create_time,
            update_time, data_status, dept_id, dept_flag, project_code, rule_code,
            business_code, version_control
        ) VALUES
        <foreach collection="fraudGroupList" item="item" separator=",">
            (
                #{item.id}, #{item.modelId}, #{item.name}, #{item.descr}, #{item.status},
                #{item.remark}, #{item.createUserId}, #{item.createTime}, #{item.updateTime},
                #{item.dataStatus}, #{item.deptId}, #{item.deptFlag}, #{item.projectCode},
                #{item.ruleCode}, #{item.businessCode}, #{item.versionControl}
            )
        </foreach>
    </insert>

</mapper>
