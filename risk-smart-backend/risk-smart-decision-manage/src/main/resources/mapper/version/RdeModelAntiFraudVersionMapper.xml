<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.value.decision.version.mapper.RdeModelAntiFraudVersionMapper">

    <!-- 批量插入反欺诈模型版本 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO rde_model_anti_fraud_version (
            id, name, descr, model_type, status, check_status, new_rule, run_rule,
            rule_type, remark, create_user_id, create_time, update_time, dept_id,
            data_status, dept_flag, rule_code, project_code, business_code, version_control
        ) VALUES
        <foreach collection="fraudList" item="item" separator=",">
            (
                #{item.id}, #{item.name}, #{item.descr}, #{item.modelType}, #{item.status},
                #{item.checkStatus}, #{item.newRule}, #{item.runRule}, #{item.ruleType},
                #{item.remark}, #{item.createUserId}, #{item.createTime}, #{item.updateTime},
                #{item.deptId}, #{item.dataStatus}, #{item.deptFlag}, #{item.ruleCode},
                #{item.projectCode}, #{item.businessCode}, #{item.versionControl}
            )
        </foreach>
    </insert>

</mapper>
